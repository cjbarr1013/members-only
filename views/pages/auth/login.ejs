<div
  class="flex w-full flex-col items-center gap-8 md:flex-row md:justify-center md:gap-16"
>
  <div class="flex max-w-130 flex-col items-center gap-2 md:items-start">
    <%- include('../../components/title', {size: 'lg'}) %>
    <h2 class="text-center text-2xl md:text-left">
      Because being a Jace is more than a name — it’s a lifestyle.
    </h2>
  </div>
  <div class="form-card"><%- include('../../forms/login') %></div>
</div>
